# Lessons

One lesson per file, named `<short-kebab-slug>.md`, with a one-line summary as the
first line. Record corrections and confirmed approaches, with why they mattered
(the incident and its date). Don't record what Git already knows. Update rather
than duplicate; delete what turns out wrong.

Lessons are a recall aid, never authority: verify against current source first.
