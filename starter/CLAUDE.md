# CLAUDE.md — delivery adapter

This file puts the repo's rules into Claude Code's context by importing
`AGENTS.md`, the cross-vendor rulebook that Codex also reads directly. Do not add
policy here: `AGENTS.md` is the only place policy lives.

@AGENTS.md
