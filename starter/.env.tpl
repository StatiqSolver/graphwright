# Variable NAMES only. Values live in the secret manager, never on disk.
#   Secret manager: <e.g. 1Password Environment "<project>-dev" in vault Dev>
#   Environment id: <recorded in the owner's secrets registry, not here if private>
#   Run with secrets: op run --environment <id> -- <command>
# Add a secret to the manager first, then its name here.

# EXAMPLE_PROVIDER_API_KEY=
